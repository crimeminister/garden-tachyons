(ns ca.clojurist.styles.tachyons.config.space
  "Spacing definitions."
  (:require
   [garden.units :refer [rem]]))

(def spacing-none 0)
(def spacing-extra-small (rem 0.25))
(def spacing-small (rem 0.5))
(def spacing-medium (rem 1))
(def spacing-large (rem 2))
(def spacing-extra-large (rem 4))
(def spacing-extra-extra-large (rem 8))
(def spacing-extra-extra-extra-large (rem 16))
